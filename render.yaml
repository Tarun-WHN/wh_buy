services:
  - type: web
    name: wh-buy
    runtime: node
    region: singapore
    plan: starter
    nodeVersion: "20"
    buildCommand: npm install && npx prisma generate && npx prisma migrate deploy && npx prisma db seed && npm run build
    startCommand: npm run start
    disk:
      name: wh-buy-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DATABASE_URL
        value: file:/data/prod.db
      - key: NEXTAUTH_URL
        value: https://wh-buy.onrender.com
      - key: NEXTAUTH_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
